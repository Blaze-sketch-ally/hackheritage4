"""Pydantic schemas for the AI subsystem. See base.py for the generic,
provider/agent-agnostic Phase-1 shapes; later phases add agent-specific
response schemas alongside these, never inside them.
"""
